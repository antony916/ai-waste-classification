# AI-Based Waste Classification System

Classifies waste images into cardboard, glass, metal, paper, plastic and trash using MobileNetV3-Large transfer learning.

Dataset: `garythung/trashnet` on Hugging Face.

Run `pip install -r requirements.txt`, then `python src/train.py`, then `streamlit run app.py`.
